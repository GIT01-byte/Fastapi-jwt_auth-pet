
# Fastapi-jwt_auth-pet

 🚀 Описание проекта
Высокопроизводительный API-сервис аутентификации на базе FastAPI, использующий современные стандарты безопасности:
•   JWT (EdDSA) — асимметричное шифрование для токенов.
•   Refresh Tokens — ротация сессий через базу данных.
•   Redis Blacklist — мгновенная инвалидация токенов при выходе (Logout).
•   HttpOnly Cookies — защита от XSS атак.

---

 ⚙️ Системные требования
•   Python 3.9+
•   Docker & Docker Compose

---

 🛠 Инструкция по запуску

 1. Клонирование репозитория
git clone https://github.com/GIT01-byte/Fastapi-jwt_auth-pet
cd Fastapi-jwt_auth-pet


 2. Настройка переменных окружения
Переименуйте файл .env.example в .env и подставьте свои значения:
# Параметры базы данных
APP__CONFIG__DB__HOST=postgres
APP__CONFIG__DB__PORT=5432
APP__CONFIG__DB__USER=user
APP__CONFIG__DB__PWD=password
APP__CONFIG__DB__NAME=my_database

# Параметры Redis
APP__CONFIG__REDIS__HOST=redis
APP__CONFIG__REDIS__PORT=6379

# Режим работы (DEV / PROD)
APP__CONFIG__MODE=DEV

Если вы запускаете проект без Docker, замените postgres и redis на localhost.

 3. Генерация ключей шифрования
Для работы алгоритма EdDSA необходимо сгенерировать пару приватного и публичного ключей:
python ./backend/auth/utils/gen_keys.py

Ключи появятся в папке сертификатов проекта.

 4. Запуск через Docker
Запустите все сервисы (API, DB, Redis) одной командой:
docker-compose up --build -d


---

 📑 Справочник API (API Reference)

Базовый адрес: http://localhost:8080/users

 1. Публичные эндпоинты
